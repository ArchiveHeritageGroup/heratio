# v1.154.490 (2026-08-03)

feat(archaeology): #1428 Phase 4b final - find-form AJAX context refresh + tree-slotting on save (Phase 4b complete). (1) contextsJson endpoint (GET /archaeology/site/{siteId}/contexts.json) + an inline script on the find form that refetches and repopulates the context picker when the site dropdown changes (was: showed only the preset site's contexts); the current selection is preserved on edit. Bare inline script is auto-nonced by InjectCspNonces, so it is CSP-safe. (2) createDescription (shared by context/site/find) now slots the auto-created child description into the information_object closure tree immediately - lft/rgt=null + ClosureMaintenanceService::addNode('information_object', ioId, parentId), matching the main IO store - instead of leaving lft/rgt=0 with no closure row (invisible in the tree until a rebuild). Verified: a fresh context's IO gets a closure self-row + a row under the site IO, and HierarchyQueryService::descendantIds(siteIo) includes it right away. Completes #1428 Phase 4 / the whole issue. No schema change.

## Files in this release

```
 .../resources/views/object-form.blade.php          | 35 ++++++++++++++++++++--
 .../src/Controllers/ArchaeologyController.php      | 10 +++++++
 .../Providers/AhgArchaeologyServiceProvider.php    |  3 ++
 .../src/Services/ArchaeologyService.php            | 20 +++++++++++--
 version.json                                       |  2 +-
 5 files changed, 63 insertions(+), 7 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.490
