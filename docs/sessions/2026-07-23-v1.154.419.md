# v1.154.419 (2026-07-23)

feat(dacs): wire DACS into the dynamic-standard form (#1425 Stage 2). Same pattern as RiC (v1.154.418): extracted the DACS field accordion into dacs-manage::_fields (nullable $io; no <form>/save/display-standard dropdown - the host owns those), refactored dacs-manage::edit to @include it, and added DacsManageController::persist() (save logic lifted from edit()'s POST branch) + fieldsPartial() (render for create/edit). DACS auto-joins the STANDARD_CONTROLLERS registry now that both methods exist, so picking 'DACS' on the archival-description create/edit form swaps in the DACS field set (Identity / Dates & creators / Content / Conditions / Allied / Notes / Access points / Description control / Administration) and Save persists via DacsManageController::persist(). Verified: swap?code=dacs returns the DACS accordion (no longer the ISAD fallback); create with DACS selected persists source_standard='DACS 2nd edition' AND the DACS-unique languageOfDescription property; the standalone DACS editor still renders via the shared partial; 2487 views compile. Stage 2 remaining: RAD, MODS, DC by the identical pattern

## Files in this release

```
 .../resources/views/_fields.blade.php              | 365 ++++++++++++++++++
 .../ahg-dacs-manage/resources/views/edit.blade.php | 350 +----------------
 .../src/Controllers/DacsManageController.php       | 412 ++++++++++++---------
 version.json                                       |   2 +-
 4 files changed, 614 insertions(+), 515 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.419
