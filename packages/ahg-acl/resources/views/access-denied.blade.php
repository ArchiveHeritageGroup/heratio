@extends('theme::layouts.1col')
@section('title', 'Access Denied')
@section('content')
<div class="container py-4">
<div class="row justify-content-center"><div class="col-md-8"><div class="card border-danger shadow"><div class="card-header bg-danger text-white"><h4 class="mb-0"><i class="fas fa-lock me-2"></i>{{ __('Access Denied') }}</h4></div><div class="card-body"><p class="lead">You do not have permission to access:</p><p class="h5 text-muted">{{ e($objectTitle??"this resource") }}</p><hr>@if(in_array("classification",$access["reasons"]??[]))<div class="alert alert-warning"><h5><i class="fas fa-shield-alt me-2"></i>{{ __('Security Classification Required') }}</h5><p class="mb-0">This material is classified as <strong>{{ e($access["classification"]["name"]??"Classified") }}</strong>.@guest<br>Please <a href="{{ route("login") }}">log in</a> to verify your clearance.@else<br>Your current clearance does not permit access.@endguest</p></div>@endif @if(in_array("donor_restriction",$access["reasons"]??[]))<div class="alert alert-info"><h5><i class="fas fa-user-shield me-2"></i>{{ __('Donor Restriction') }}</h5><p class="mb-0">Access restricted by donor agreement.</p></div>@endif @if(in_array("embargo",$access["reasons"]??[]))<div class="alert alert-secondary"><h5><i class="fas fa-clock me-2"></i>{{ __('Under Embargo') }}</h5><p class="mb-0">Embargoed until <strong>{{ date("F j, Y",strtotime($access["embargo"]["end_date"])) }}</strong>.</p></div>@endif<hr><div class="d-flex justify-content-between"><a href="javascript:history.back()" class="btn btn-outline-secondary"><i class="fas fa-arrow-left me-1"></i>{{ __('Go Back') }}</a>@guest<a href="{{ route("login") }}" class="btn atom-btn-white"><i class="fas fa-sign-in-alt me-1"></i>{{ __('Log In') }}</a>@else<a href="{{ route("security.my-requests") }}" class="btn atom-btn-white"><i class="fas fa-hand-paper me-1"></i>{{ __('Request Access') }}</a>@endguest</div></div></div></div></div>
</div>
@endsection
