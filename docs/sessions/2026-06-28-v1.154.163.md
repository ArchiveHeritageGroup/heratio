# v1.154.163 (2026-06-28)

fix(ahg-image-ar/ahg-3d-model/ahg-federation): close IDOR/hotspot-leak + federation SSRF (#1361, #1380) — image-ar delete now requires admin (was any authed user deleting any animation+MP4 by id); 3d-model legacy hotspot aliases now require admin (matched the canonical routes); apiHotspots returns empty for anon on a non-public model (was leaking hotspot metadata). ahg-federation testPeer() now runs the admin-supplied URL through FederationClient::hostAllowed() (blocks private/metadata hosts — verified 169.254.169.254 rejected) + withoutRedirecting() on the probes. Verified: route:list RequireAdmin, hostAllowed false on metadata host. (#1361 image-ar gateway-URL + #1380 plaintext-creds encryption deferred; both stay open.)

## Files in this release

```
A  docs/sessions/2026-06-28-v1.154.163.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.163
