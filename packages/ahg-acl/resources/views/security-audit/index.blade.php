@extends('theme::layouts.1col')
@section('title', 'Security Audit Logs')
@section('content')
<div class="container py-4">
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1><i class="fas fa-clipboard-list me-2"></i>Security Audit Logs</h1><p class="text-muted mb-0">Showing {{ count($logs??[]) }} of {{ number_format($total??0) }} records</p></div><a href="{{ route("acl.security-audit-dashboard") }}" class="btn atom-btn-white"><i class="fas fa-chart-line me-1"></i>{{ __('Dashboard') }}</a></div><div class="card mb-4"><div class="card-header bg-light"><h5 class="mb-0"><i class="fas fa-filter me-2"></i>Filters</h5></div><div class="card-body"><form method="get" class="row g-3"><div class="col-md-2"><label class="form-label small">User <span class="badge bg-secondary ms-1">{{ __('Optional') }}</span></label><input type="text" name="user" class="form-control form-control-sm" value="{{ request("user") }}"></div><div class="col-md-2"><label class="form-label small">Action <span class="badge bg-secondary ms-1">{{ __('Optional') }}</span></label><select name="log_action" class="form-select form-select-sm"><option value="">{{ __('All') }}</option>@foreach($actions??[] as $act)<option value="{{ $act }}" {{ request("log_action")===$act?"selected":"" }}>{{ $act }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label small">From <span class="badge bg-secondary ms-1">{{ __('Optional') }}</span></label><input type="date" name="date_from" class="form-control form-control-sm" value="{{ request("date_from") }}"></div><div class="col-md-2"><label class="form-label small">To <span class="badge bg-secondary ms-1">{{ __('Optional') }}</span></label><input type="date" name="date_to" class="form-control form-control-sm" value="{{ request("date_to") }}"></div><div class="col-md-2 d-flex align-items-end"><button type="submit" class="btn atom-btn-white btn-sm"><i class="fas fa-search me-1"></i>{{ __('Filter') }}</button></div></form></div></div><div class="card"><div class="card-body p-0"><table class="table table-hover table-striped mb-0"><thead><tr><th>{{ __('Date/Time') }}</th><th>{{ __('User') }}</th><th>{{ __('Action') }}</th><th>{{ __('Category') }}</th><th>{{ __('Object') }}</th><th>{{ __('Details') }}</th></tr></thead><tbody>@forelse($logs??[] as $log)<tr><td><small>{{ $log->created_at??"" }}</small></td><td>{{ e($log->username??"") }}</td><td><span class="badge bg-secondary">{{ e($log->action??"") }}</span></td><td><small>{{ e($log->category??"") }}</small></td><td>{{ e($log->object_title??"") }}</td><td><small class="text-muted">{{ \Illuminate\Support\Str::limit($log->details??"",80) }}</small></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-3">No audit log entries.</td></tr>@endforelse</tbody></table></div></div>
</div>
@endsection
