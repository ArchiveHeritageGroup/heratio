# v1.154.595 (2026-08-13)

fix(preservation)+feat(exhibition): #1139 TIFF/RAW access copies. Preservation: NormalizationService handed encrypted-at-rest masters straight to the CLI tool, so every one failed on ciphertext with a misleading 'Not a TIFF, bad magic number 18497' (0x4841 is the HA of AHG-ENC). It now decrypts a Heratio-envelope master to a private temp file first, mirroring ahg-media-processing DerivativeService, and deletes that plaintext in a finally on every exit path; a FOREIGN AHG-ENC-V2 envelope we hold no key for is detected via isFileEncryptedAtRest and skipped with a clear log line instead of a bogus conversion attempt. Note isFileEncryptedAtRest not isFileEncrypted - turning encryption off does not decrypt files already written, so the config flag is not the question. Exhibition: new ahg:exhibition-normalize-images {--space} {--sync} {--dry-run} queues access copies for objects placed in a space that the walkthrough cannot draw, delegating entirely to the preservation registry (no second converter); soft-gated on ahg-preservation via class_exists. It skips objects that render as 3D/splat/PDF rather than counting them as broken, and reports failures separately from successes with this run's error, not a stale one

## Files in this release

```
 .../Commands/ExhibitionNormalizeImagesCommand.php  | 209 +++++++++++++++++++++
 .../src/Providers/AhgExhibitionServiceProvider.php |   1 +
 .../src/Services/NormalizationService.php          |  63 ++++++-
 version.json                                       |   2 +-
 4 files changed, 273 insertions(+), 2 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.595
