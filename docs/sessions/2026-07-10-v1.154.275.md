# v1.154.275 (2026-07-10)

fix(archivematica): start_transfer path encoding — encode base64("{transfer_source_LOCATION_uuid}:{path}") using the SS location UUID (not the pipeline UUID), and hand-build the form body so repeated paths[]/row_ids[] keys reach AM's getlist('paths[]') (form_params serialised them as paths[][0]= → 'No path provided'). Adds am_transfer_source_location_uuid config. Verified: the transfer now resolves the SS Transfer-Source location and reaches file staging.

## Files in this release

```
A  docs/sessions/2026-07-10-v1.154.275.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.275
