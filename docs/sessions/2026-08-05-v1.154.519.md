# v1.154.519 (2026-08-05)

feat(#1456): move the Administration area to the bottom of the archival edit form. The #admin-area accordion (description-standard driver, publication status, collection type, source language, last-updated) was pulled from the top of the form to the bottom - it now renders immediately after the main field accordion closes, just above the Save/Cancel actions, i.e. directly above the Digital objects upload/manage block that follows </form>. Stays inside <form> (its fields are submitted) and outside #standard-fields (so the standard-swap driver still persists across an AJAX field-set swap, per #1425); the data-standard-driver dropdown works unchanged from the new position (binds by selector). Verified: rendered order is standard-fields < admin-area < actions < digitalObjectAccordion; driver present; view:cache compiles.

## Files in this release

```
 .../resources/views/edit.blade.php                 | 159 +++++++++++----------
 version.json                                       |   2 +-
 2 files changed, 82 insertions(+), 79 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.519
