@extends('theme::layouts.1col')
@section('title', 'Intake Configuration')
@section('content')
<div class="container py-4">
<nav aria-label="breadcrumb" class="mb-3"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route("accession.dashboard") }}">Accessions</a></li><li class="breadcrumb-item active">Intake Configuration</li></ol></nav><h1><i class="fas fa-cog me-2"></i>Intake Configuration</h1><div class="card"><div class="card-body"><form method="post" action="{{ route("accession.intake-config-store") }}">@csrf<div class="row"><div class="col-md-6 mb-3"><label class="form-label">Default Processing Status <span class="badge bg-secondary ms-1">Optional</span></label><select name="default_status_id" class="form-select">@foreach($statuses??[] as $s)<option value="{{ $s->id }}" {{ ($config->default_status_id??0)==$s->id?"selected":"" }}>{{ e($s->name) }}</option>@endforeach</select></div><div class="col-md-6 mb-3"><label class="form-label">Default Priority <span class="badge bg-secondary ms-1">Optional</span></label><select name="default_priority_id" class="form-select">@foreach($priorities??[] as $p)<option value="{{ $p->id }}" {{ ($config->default_priority_id??0)==$p->id?"selected":"" }}>{{ e($p->name) }}</option>@endforeach</select></div></div><button type="submit" class="btn atom-btn-white"><i class="fas fa-save me-1"></i>Save</button></form></div></div>
</div>
@endsection
