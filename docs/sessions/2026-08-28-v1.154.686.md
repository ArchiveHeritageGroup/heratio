# v1.154.686 (2026-08-28)

docs: session handover for 25-28 Aug (v1.154.674-685) - written for a session restart. Carries the judgement the per-release notes do not: why each change was made, what I got wrong, and what is still open. THE THROUGH-LINE was one defect class - a page bound to something its controller never produces, failing silently into an empty state rather than an error. Three more instances of it turned up in MuseumController alone (provenance, object comparison, quality dashboard), the last reporting a permanent 0% catalogue score regardless of the catalogue, which asserts a measurement nobody took. The handover says where to look for the rest: grep for '= collect();' and '= 0;' in controller methods that return a view. FIVE THINGS I GOT WRONG are recorded as prominently as the fixes, because each one cost real time and would cost it again: telling a peer session Heratio had a working provenance display when the page I pointed at was itself a stub; writing a confidently wrong claim about set -e semantics into a script comment and only then testing it; calling the mysqldump failures transient for months on no evidence when they were deterministic to within 2 KB; sizing the VM backup rotation from du, which reports allocated rather than real size, turning a 17-minute job into a 3.5-hour estimate; and shipping a due-diligence card that rendered for objects with no concerns because 'none' is a non-empty string. Also records the rc=3 root cause and the part of it that remains unexplained, the 01:00 deploy hazard that broke a dump attempt, the fact that anything below git push in bin/release is dead code on dev, and my read on each open issue including which one was filed on a false premise. Dropped into /var/spool/km-ingest and committed so the indexer crawls it from both surfaces.

## Files in this release

```
 docs/sessions/2026-08-28-session-handover.md | 123 +++++++++++++++++++++++++++
 version.json                                 |   2 +-
 2 files changed, 124 insertions(+), 1 deletion(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.686
