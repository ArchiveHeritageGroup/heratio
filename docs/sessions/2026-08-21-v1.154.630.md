# v1.154.630 (2026-08-21)

fix(errorlog): collapse a repeating fault into one row with an occurrence count instead of a new row every time. bootstrap/app.php inserted a row into ahg_error_log for every reported throwable with no deduplication - fine for distinct faults, useless for a persistent one. While the AI gateway key sat at 0 remaining, the scheduler failed every 5 minutes and wrote the identical 'Scheduled command [ahg:cron-run] failed with exit code [1]' row 13 times an hour on each of two instances; clearing the log refilled it within minutes because nothing about the condition had changed, only the row count. 76 identical rows is alert fatigue, and it buries the one-off errors that actually need reading. New migration adds fingerprint (sha1 of exception_class|message|file|line, indexed), occurrences and last_seen_at; the reporter now UPDATEs the open row for a recurring fingerprint - bumping occurrences and last_seen_at, marking it unread again, and leaving created_at as the FIRST sighting - and only INSERTs for a fault that is new or that an operator has already resolved, so a resolved row never silently absorbs new occurrences. Guarded with a cached Schema::hasColumn so the deploy window before the migration runs cannot fatal. The admin view shows the count and last-seen beside the timestamp, because a collapsed row must not read as a single event. Verified: 7 reports of 2 distinct faults produce exactly 2 rows (occ=5 and occ=2), and re-reporting a resolved fault opens a new row. Nothing is hidden - a repeating error still shows, with its true count and the span it covers, which is more than 76 identical rows carried. 231 Feature tests pass

## Closes
- [#1470](https://github.com/ArchiveHeritageGroup/heratio/issues/1470)

## Files in this release

```
 bootstrap/app.php                                  | 46 +++++++++++++-
 ...1_090000_add_occurrence_dedupe_to_error_log.php | 71 ++++++++++++++++++++++
 .../resources/views/errorLog.blade.php             | 10 +++
 version.json                                       |  2 +-
 4 files changed, 126 insertions(+), 3 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.630
