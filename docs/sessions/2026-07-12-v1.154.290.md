# v1.154.290 (2026-07-12)

security(authz): access-control hardening from the full-codebase sweep - DAM write routes now require acl:create/update/delete (was auth-only, any user could delete catalog records that cascade into information_object); draft/restricted-record exports (EAD/DC/CRM/OAI) gated with the show() per-record read check and OAI descendants filtered to published; API read scope now derived from ACL capability instead of auto-granted to every session, and actor/research-project API contact PII restricted to admins; per-record write ACL enforced on information-object and actor update/move/rename/delete (object-level DENY parity with read); the 19 project-scoped research controllers now enforce the #1308 project-membership gate (closes cross-researcher IDOR) via a shared trait; access-request/provenance/modifications/loan/jstree IDOR disclosures gated; WhatsApp webhook no longer fails open on an empty secret; Cart PayFast ITN routed through the marketplace verifyItn (signature + source-IP + server-to-server validate + amount check)

## Files in this release

```
A  docs/sessions/2026-07-12-v1.154.290.md
A  packages/ahg-research/src/Concerns/AuthorizesProjectAccess.php
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.290
