# v1.154.453 (2026-07-31)

fix(preservation): PreservationService::exportPackage (the 'Run the BagIt export job' path) had the same doubled-/uploads/ bug - resolveSourcePath treated the web path /uploads/... as a filesystem path and its glob fallback didn't recurse, so it logged 'missing source file' for import layouts like /uploads/r/<repo>/<hash>/; strip the prefix, join uploads_path, and widen the glob

## Files in this release

```
 .../src/Services/PreservationService.php           | 44 ++++++++++++++++++----
 version.json                                       |  2 +-
 2 files changed, 37 insertions(+), 9 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.453
