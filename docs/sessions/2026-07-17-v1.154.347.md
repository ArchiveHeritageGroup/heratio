# v1.154.347 (2026-07-17)

fix(taxonomy): #1388 - TermProtocolGate query scopes (addTermVisibilityCriteria/excludeRestrictedRecords) now guard on Schema::hasTable('term_protocol') and no-op when the table is absent, so a fresh install or the CI test schema (which loads database/core/*.sql, not migrations) no longer 500s the RiC records API / OAI / display gates. Also mirror the term_protocol table into database/core/00_core_schema.sql (matching the portable_export_share_token precedent) so CI + fresh schema installs carry it and TermProtocolGateTest runs instead of skipping. Fixes the DraftRecordVisibilityTest 'guest ric records api excludes draft' 500 that was failing both Heratio Test Suite and E2E Parity

## Files in this release

```
A  docs/sessions/2026-07-17-v1.154.347.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.347
