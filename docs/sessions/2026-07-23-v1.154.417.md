# v1.154.417 (2026-07-23)

feat(ric): RiC-O download format in metadata-export (#1425 A2 tail). Adds RiC-O (Records in Contexts) alongside the CIDOC-CRM/PROV-O RDF exports: GET /admin/metadata-export/ric?io=NNN (Turtle default) and /ric.rdf?io=NNN (RDF/XML), mirroring downloadCidocCrm's shape exactly. Reuses the ahg/ric engine - RicSerializationService::serializeRecord -> the same toTurtle()/toRdfXml() static serialisers the OAI rico prefix uses (v1.154.414), no new serialisation code. Guarded by class_exists(RicSerializationService): 404s cleanly when the engine is absent, preserving the standalone-install guarantee. Verified: Turtle output carries @prefix directives (1091 bytes for the test record), RDF/XML is well-formed with proper application/rdf+xml + attachment filename headers, both HTTP 200; routes ahgmetadataexport.ric[.ext] registered. Parallels the existing CIDOC-CRM download (also a direct-URL endpoint, not a view link)

## Files in this release

```
 packages/ahg-metadata-export/routes/web.php        |  8 ++++
 .../src/Controllers/MetadataExportController.php   | 54 ++++++++++++++++++++++
 version.json                                       |  2 +-
 3 files changed, 63 insertions(+), 1 deletion(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.417
