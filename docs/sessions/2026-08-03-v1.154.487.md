# v1.154.487 (2026-08-03)

feat(archaeology): #1428 Phase 4b - CSV import of stratigraphic contexts + relationships. ArchaeologyService::importContextsCsv() upserts contexts by (site, context_number) then resolves the relationship columns (above/below/cuts/cut_by/fills/filled_by/same_as/bonds_with/abuts, values = other context numbers) via addRelationship so reciprocity + the cycle guard are reused; the whole run is a DB transaction rolled back when not committing, so a preview reports exact created/updated/relationship counts and every warning without writing. Controller contextImportForm/contextImport/contextImportTemplate + a BOM-tolerant parseCsv (lower-cases the header, requires a context_number column); type/phase matched by name against the site vocab (unknown left blank + warned). New context-import view (upload + Save-changes=commit-else-preview + result card + downloadable CSV template with a worked example row); Import CSV button on the Stratigraphy page. Verified: preview rolls back, commit creates with the correct above/below mirror, idempotent re-import (no duplicate edges), unknown type/ref/blank rows warn. No schema change (reuses the Phase 1/2 tables).

## Files in this release

```
 .../resources/views/context-import.blade.php       | 108 +++++++++++++
 .../resources/views/contexts.blade.php             |   5 +-
 .../src/Controllers/ArchaeologyController.php      | 122 +++++++++++++++
 .../Providers/AhgArchaeologyServiceProvider.php    |   9 ++
 .../src/Services/ArchaeologyService.php            | 170 +++++++++++++++++++++
 version.json                                       |   2 +-
 6 files changed, 414 insertions(+), 2 deletions(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.487
