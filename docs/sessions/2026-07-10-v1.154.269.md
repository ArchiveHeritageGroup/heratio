# v1.154.269 (2026-07-10)

fix(#1357): portable-export share links now work + are safe — apiToken() returned a /portable-export/share/{token} URL with no route registered (every link 404'd). Add the anonymous token-gated share route + share() handler (128-bit token, expiry, download cap, revoke-aware); add the portable_export_share_token table (new plugin table); record included_unpublished in the bundle disclosure_summary; gate public links to PUBLISHED-ONLY bundles — refused at both mint (apiToken 422) and serve (403). ICIP/TK, ODRL and PII are already excluded at build time. No base-AtoM table changes.

## Files in this release

```
A  docs/sessions/2026-07-10-v1.154.269.md
A  packages/ahg-portable-export/database/migrations/2026_07_10_000000_create_portable_export_share_token.php
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.269
