# v1.154.289 (2026-07-12)

security(hardening): non-authz fixes from the full-codebase sweep - (1) XSS: add encoder=html to all 5 Elasticsearch highlight blocks so raw HTML in record/book titles is escaped in search results, strip HTML from blog markdown, escape the favourite-folder dropdown JSON; (2) uploads: deny browser-executable extensions (html/svg/php/js/...) at every digital-object upload entry point plus the DigitalObjectService choke point (was stored-XSS / latent RCE); (3) SSRF: route the caption fetch and the AI-services/central test-connection probes through the in-repo SsrfGuard and disable redirect-following on the two file_get_contents importers; (4) open redirect on login ?next= closed with a same-host allow-list matching /set-locale; (5) whitelist sort direction+column on 3 unvalidated orderBy sites

## Files in this release

```
A  docs/sessions/2026-07-12-v1.154.289.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.289
