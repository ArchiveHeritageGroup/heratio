# v1.154.238 (2026-07-05)

ci(deploy): add ./bin/deploy-check post-deploy guard — verifies vendor/ is in sync with composer.lock (catches the code-pull-without-composer-install gap that left prod on the still-vulnerable phpspreadsheet 5.5.0 after the #1395/F lock bump until caught), that composer audit has no high/critical advisory, and that the app boots (artisan about); --fix runs composer install + package:discover to resync. Now the mandatory final step of the prod deploy runbook.

## Files in this release

```
A  bin/deploy-check
A  docs/sessions/2026-07-05-v1.154.238.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.238
