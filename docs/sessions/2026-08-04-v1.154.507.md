# v1.154.507 (2026-08-04)

feat(finding-aid): full-detail model now embeds reference-copy images. In full-details finding aids, the described unit and each descendant with a reference derivative (digital_object usage_id 141, thumbnail 142 or the image master as fallback) now render a <dao> EAD element in the XML and an embedded <img> in the PDF. Reference-copy path resolved via config(heratio.uploads_path) (mirrors RedactionRenderService); wkhtmltopdf gains --enable-local-file-access so file:// images embed (was silently dropped). Inventory-summary model unchanged (gated on !isInventory). Verified: io 837 single image + collection 904381 embeds 27 reference copies top+children

## Files in this release

```
 app/Jobs/FindingAidJob.php | 77 ++++++++++++++++++++++++++++++++++++++++++++++
 version.json               |  2 +-
 2 files changed, 78 insertions(+), 1 deletion(-)
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.507
