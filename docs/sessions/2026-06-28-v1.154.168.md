# v1.154.168 (2026-06-28)

fix(#1368): route Ollama LLM completion through the AHG AI gateway, not a direct :11434 node — LlmService dispatch + health now resolveOllamaBase() (rejects raw-node endpoint_url, falls back to ai.theahg.co.za/ai/v1) and callOllama POSTs {base}/ollama/api/generate with the gateway Bearer key; reseed install.sql api_url/endpoint_url to the gateway. Fixes chatbot completion + ai.describe bypass at the shared layer. Verified end-to-end (completeFull -> gateway -> mistral:7b). Donut extraction deferred (#1368 stays open — gateway has no /donut route yet).

## Files in this release

```
A  docs/sessions/2026-06-28-v1.154.168.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.168
