# v1.154.348 (2026-07-17)

ci: build-install-artifact now creates the GitHub Release object explicitly before uploading schema/seed assets. bin/release only pushes a tag (never a Release), so softprops/action-gh-release@v2 intermittently failed fetching a non-existent release (HTML HttpError) - replaced with an idempotent gh CLI step (gh release view || gh release create --verify-tag, then gh release upload --clobber). Fixes the recurring build-install-artifact red

## Files in this release

```
A  docs/sessions/2026-07-17-v1.154.348.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.348
