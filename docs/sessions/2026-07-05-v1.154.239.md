# v1.154.239 (2026-07-05)

security(#1395 leftovers): close the remaining LOW/MEDIUM sweep items. (E-LOW) OAI ?api= key no longer leaks into logs — new AhgCore\Support\LogRedactor redacts credential query params (api/key/token/secret/...), applied at the global ahg_error_log.url sink (bootstrap/app.php), the request-trace span (RequestIdMiddleware), and the voice audit log. (C-MEDIUM) WARC capture SSRF redirect gap closed — WarcCaptureService now follows redirects MANUALLY, re-validating every hop (initial + each 30x target) through the shared SsrfGuard (DNS-resolve + per-IP + decimal-host); subresources guarded + no-follow. (B-MEDIUM) escaped two raw-render stored-XSS sinks: accession alternative-identifier note + function description (nl2br(e())). Repository htmlSnippet left as-is (acl:update-gated admin HTML); privacy banner already escaped. Verified: redaction cases, SsrfGuard blocks metadata/loopback/decimal/IPv6 + allows public, WARC fetch drives real 200 + http->https redirect end-to-end.

## Closes
- [#1395](https://github.com/ArchiveHeritageGroup/heratio/issues/1395)

## Files in this release

```
A  docs/sessions/2026-07-05-v1.154.239.md
A  packages/ahg-core/src/Support/LogRedactor.php
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.239
