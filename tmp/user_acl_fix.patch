*** Begin Patch
*** Update File: packages/ahg-research/src/Contracts/UserProvisionerInterface.php
@@
     public function findByEmail(string $email): ?object;
 
+    /**
+     * Check whether a user_id is a member of a given ACL group.
+     *
+     * @param int $userId
+     * @param int $groupId
+     * @return bool
+     */
+    public function isInGroup(int $userId, int $groupId): bool;
  }
*** End Patch

*** Begin Patch
*** Update File: packages/ahg-research/src/Services/UserProvisioner/EloquentUserProvisioner.php
@@
     public function findByEmail(string $email): ?object
     {
         return DB::table('user')->where('email', $email)->first();
     }
+
+    /**
+     * Check whether a user_id is a member of a given ACL group.
+     *
+     * @param int $userId
+     * @param int $groupId
+     * @return bool
+     */
+    public function isInGroup(int $userId, int $groupId): bool
+    {
+        return DB::table('acl_user_group')
+            ->where('user_id', $userId)
+            ->where('group_id', $groupId)
+            ->exists();
+    }
  }
*** End Patch

*** Begin Patch
*** Update File: packages/ahg-research/src/Middleware/OdrlPolicyMiddleware.php
@@
     private function isAdmin(int $userId): bool
     {
-        return DB::table('acl_user_group')
-            ->where('user_id', $userId)
-            ->where('group_id', 100) // Administrator group
-            ->exists();
+        // Prefer centralized ACL check via the UserProvisioner when available.
+        $prov = app(\AhgResearch\Contracts\UserProvisionerInterface::class);
+        if (method_exists($prov, 'isInGroup')) {
+            return $prov->isInGroup($userId, 100);
+        }
+
+        // Fallback to direct DB check for backward compatibility.
+        return DB::table('acl_user_group')
+            ->where('user_id', $userId)
+            ->where('group_id', 100) // Administrator group
+            ->exists();
     }
*** End Patch

*** Begin Patch
*** Update File: packages/ahg-research/src/Controllers/ResearchController.php
@@
-                DB::table('user')->where('id', $researcher->user_id)->update(['active' => 1]);
+                // Use the central UserProvisioner to mutate core user rows.
+                app(\AhgResearch\Contracts\UserProvisionerInterface::class)
+                    ->updateUser($researcher->user_id, ['active' => 1]);
*** End Patch
