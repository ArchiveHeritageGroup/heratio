# v1.154.182 (2026-06-29)

feat(provenance): split provenance from museum — make it sector-neutral across all catalogue sectors. Route de-CCO'd: io.provenance now resolves to /{slug}/provenance (legacy /{slug}/cco/provenance kept as io.provenance.cco alias), so every existing route('io.provenance') caller (museum/IO/DAM) is de-museumed at once; backend unchanged (provenance_entry is already information_object_id-keyed). Surfaced the neutral 'Provenance' link on the catalogue sectors that lacked it — ahg-gallery (artwork), ahg-library (item), ahg-accession-manage (per linked description). Object-provenance is IO-keyed so it applies to catalogue records, not operational/authority records (loan/storage/exhibition/vendor/actor/repository). Verified: /{slug}/provenance + legacy /cco/ both 200, no regression, blades lint clean.

## Files in this release

```
A  docs/sessions/2026-06-29-v1.154.182.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.182
