@php $access=$access??[]; $userContext=$userContext??[]; if(empty($access)) return; $classColors=["PUBLIC"=>"success","INTERNAL"=>"info","CONFIDENTIAL"=>"primary","SECRET"=>"warning","TOP_SECRET"=>"danger"]; @endphp
<div class="access-control-banner mb-3 p-2 border rounded bg-light"><div class="d-flex flex-wrap align-items-center gap-2">@if(!empty($access["classification"]))@php $code=$access["classification"]["code"]??"PUBLIC"; @endphp<span class="badge bg-{{ $classColors[$code]??"secondary" }}"><i class="fas fa-shield-alt me-1"></i>{{ e($access["classification"]["name"]??$code) }}</span>@endif @if(!empty($access["donor_restrictions"]))@foreach($access["donor_restrictions"] as $r)<span class="badge bg-warning text-dark"><i class="fas fa-user-shield me-1"></i>{{ e(ucwords(str_replace("_"," ",$r["type"]??""))) }}</span>@endforeach @endif @if(!empty($access["embargo"]))<span class="badge bg-secondary"><i class="fas fa-clock me-1"></i>Embargoed until {{ date("M j, Y",strtotime($access["embargo"]["end_date"])) }}</span>@endif @if(!empty($userContext["is_administrator"]))<span class="badge bg-dark ms-auto"><i class="fas fa-user-cog me-1"></i>{{ __('Admin View') }}</span>@endif</div></div>