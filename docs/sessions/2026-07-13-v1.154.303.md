# v1.154.303 (2026-07-13)

fix(chatbot): (1) citation chips - build the source list from the retrieved records regardless of the grounding-mode result, so an inline [N] citation in the reply always has a matching source chip (was only attached when the guardrail grounding check ran, leaving dangling [N] with no chip); (2) widget layout - add min-height:0 to the scrollable message log so it can shrink and scroll inside the flex-column card (long conversations were overflowing and pushing the input below the page bottom), and cap the panel to max-height:calc(100vh - 108px) so it always fits the viewport

## Files in this release

```
A  docs/sessions/2026-07-13-v1.154.303.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.303
