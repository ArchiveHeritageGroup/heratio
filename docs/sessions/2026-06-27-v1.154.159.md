# v1.154.159 (2026-06-27)

fix(ahg-backup/ahg-jobs): restrict DB-dump/restore to administrators + confine backup_path (#1383); defensive admin-gate on ahg-jobs (#1382, not-live) — ahg-backup download/restore/doRestore/destroy/saveSettings now require AclService::isAdministrator (was the EDITOR-inclusive canAdmin), so a full-DB dump + restore are administrators-only; saveSettings rejects a backup_path inside a web-served dir (/uploads, public/) to prevent an anon-dump-via-repoint. ahg-jobs /jobs group admin-gated defensively (its routes aren't registered at runtime, so #1382 wasn't live). Restore typed-confirm maker-checker deferred (needs a view change; #1383 stays open).

## Files in this release

```
A  docs/sessions/2026-06-27-v1.154.159.md
```

## Release link

https://github.com/ArchiveHeritageGroup/heratio/releases/tag/v1.154.159
