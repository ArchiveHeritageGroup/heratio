@extends('theme::layouts.1col')
@section('title', 'Batch Capture')
@section('content')
<div class="container py-4">
<h1><i class="fas fa-barcode me-2"></i>Batch Capture -- ISBN Lookup</h1><div class="card mb-4"><div class="card-header"><i class="fas fa-file-invoice me-2"></i>Purchase Order (Optional)</div><div class="card-body"><form method="get"><div class="row align-items-end"><div class="col-md-6"><label class="form-label">Link to PO <span class="badge bg-secondary ms-1">Optional</span></label><select name="order_id" class="form-select"><option value="">-- No PO --</option>@foreach($orders??[] as $o)<option value="{{ $o->id }}" {{ ($selectedOrderId??0)==$o->id?"selected":"" }}>{{ e($o->order_number??"") }}</option>@endforeach</select></div><div class="col-md-3"><button type="submit" class="btn atom-btn-white w-100">Select</button></div></div></form></div></div><div class="card"><div class="card-header"><i class="fas fa-barcode me-2"></i>ISBN Lookup</div><div class="card-body"><form method="post" action="{{ route("library.batch-capture-lookup") }}">@csrf<div class="mb-3"><label class="form-label">ISBNs (one per line) <span class="badge bg-secondary ms-1">Optional</span></label><textarea name="isbns" class="form-control" rows="5">{{ $rawIsbns??"" }}</textarea></div><button type="submit" class="btn atom-btn-white"><i class="fas fa-search me-1"></i>Lookup</button></form></div></div>
</div>
@endsection
